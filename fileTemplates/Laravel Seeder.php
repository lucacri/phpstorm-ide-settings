<?php use Illuminate\Database\Seeder;

use Faker\Factory as Faker;
		
class ${NAME} extends Seeder
{
	public function run() {
	    // \DB::table('domains')->delete();
        ${DS}faker = Faker::create();
        
        foreach(range(0,10) as ${DS}i){
            
        }
	}
}
